# XAUUSD 量化系统 AI 增强回测体系改造计划

## 一、现状分析

### 我们现有的优势
1. **完整的三轨架构**：DataFactory（轨1）→ 策略员（轨2）→ 运动员（轨3）
2. **统一的指标计算**：DataFactory 已计算 27+ 标准指标，所有策略共用
3. **已有 AI 基础**：llm_provider.py + ai_service.py 支持多模型 LLM
4. **Web 监控面板**：实时监控、配置热加载、策略池管理
5. **基本回测体系**：基于 Backtrader 或纯 Python 的逐根 K 线回测
6. **纸面交易桥接**：无风险验证环境

### 现有回测体系的局限性
1. **策略依赖人工编写**：需要开发者手写 `.py` 策略文件
2. **评估单一**：主要看净盈亏，缺少 IC、稳定性、熵等维度
3. **参数固定**：策略参数硬编码，缺少自动搜索
4. **结果分散**：不同脚本输出格式各异，缺少统一看板
5. **AI 未闭环**：虽有 AI 能力，但未用于策略生成或自动优化

## 二、AlphaMaster 核心方法论提炼

AlphaMaster 的价值不在于具体代码，而在于其 **自动化策略发现闭环**：

```
AI 生成策略公式 → 回测打分（多维度） → AI 分析得失 → 生成改进公式 → 迭代
```

其核心创新点：
1. **公式化策略表示**：策略不是代码，而是可执行的公式序列
2. **词表驱动**：特征（指标）+ 操作符 的有限词表
3. **虚拟机执行**：StackVM 根据公式序列计算因子值
4. **多维度评分**：IC（信息系数）、稳定性、熵、收益率综合
5. **Walk-forward 验证**：严格的 out-of-sample 测试防止过拟合
6. **精英回放+熵控制**：平衡探索与开发，避免早期收敛
7. **自适应重启**：逃离局部最优陷阱
8. **完整训练历史**：每一步的所有指标可追溯、可视化

## 三、改造方案：在现有架构上 AI 增强

我们不需要完全替换现有系统，而是 **在策员（轨2）层引入 AI 增强**，保持 DataFactory 和 运动员不变。

### 目标状态
```
DataFactory（轨1） → [AI策略员（轨2增强版）] → 运动员（轨3）
                              ↓
                     AI自动生成/优化策略
                              ↓
                    多维度回测评分 + AI分析
```

### 关键改造点

#### 1. 策员（轨2）增强：从人工策略文件 → AI 公式执行器
- **不再读取** `strategies/YYYYMMDD_name_vN.py`
- **改为读取** `strategies/best_{symbol}.json`（AI 生成的最优公式）
- 新增 `AIFormulaExecutor` 类，负责：
  - 加载公式词表（从 DataFactory 获取的标准指标）
  - 加载操作符词表（数学/逻辑运算符）
  - 使用 StackVM 执行公式序列，输出因子值（-1~1 表示仓位方向/强度）
  - 与现有运动员接口保持兼容：返回 `signal_direction` 和 `signal_strength`

#### 2. 评估机制升级：单一盈亏 → 多维度评分
在引擎核心循环中（`engine_standalone/core_loop.py` 或 `main.py`）：
- 保留现有盈亏计算
- 新增：
  - **IC 评分**：因子值与未来收益的相关系数（ roll_window=20 ）
  - **稳定性评分**：因子值的均值回归特性（如自相关系数衰减速度）
  - **熵评分**：策略多样性（防止永远只做多或只做空）
  - **风险调整收益**：夏普比率、索提诺比率
- 综合得分 = w1*收益 + w2*IC + w3*稳定性 + w4*熵（权重可配置）

#### 3. AI 自动优化循环：新增后台训练服务
新增服务：`services/ai_strategy_trainer.py`
- **输入**：历史行情数据（从 DataFactory 或数据库）
- **过程**：
  1. 调用 LLM（通过 `llm_provider.py`）生成初始策略公式
  2. 使用我们的回测引擎（复用现有 backtest/）评估该公式
  3. 将评分、公式、得失分析喂给 LLM
  4. LLM 生成改进公式
  5. 迭代 N 步（如 50 步）
- **输出**：更新 `strategies/best_{symbol}.json`
- **触发方式**：
  - 定时触发（如每 4 小时）
  - 性能退化触发（当前策略评分低于阈值）
  - 手动触发（Web Dashboard 按钮）

#### 4. 公式系统适配：利用我们现有的 27+ 标准指标
AlphaMaster 的词表需要适配我们的指标体系：
- **特征词表**（Feature Vocab）：来自 DataFactory 的 27+ 指标
  - rsi, mfi, adx, pdi, ndi, macd, macd_signal, stoch_k, stoch_d
  - bb_upper, bb_mid, bb_lower, bb_width, bb_width_direction
  - ema_9, ema_21, sma_14, sma_20, sma_50, atr, atr_20
  - volume_sma_20, price_position, trend, close
  - 以及我们自定义的：bbi, cci（如果需要添加）
- **操作符词表**（Operator Vocab）：
  - 基础运算：+ - * / %
  - 逻辑运算：> < >= <= == !=
  - 时间序列：REF(shift), DELAY, SMA, EMA, STD, etc.
  - 特殊：CLAMP, ZSCORE, RANK, etc.

#### 5. Web Dashboard 增强：AI 训练监控面板
在 `dashboard/backend/routes/` 新增：
- `/api/ai-train/status`：训练进度、当前步数、最佳得分
- `/api/ai-train/start`：手动启动训练
- `/api/ai-train/stop`：停止训练
- `/api/ai-train/history`：训练历史曲线（得分、熵、IC 等）
- `/api/ai-train/formula`：当前最优公式及其解码可视化
- 前端页面：实时显示训练进度、公式演变、性能对比

#### 6. 配置热加载：支持 AI 训练参数
在 `dashboard/runtime_config.json` 新增：
```json
{
  "ai_training": {
    "enabled": true,
    "train_steps": 50,
    "batch_size": 16,
    "eval_workers": 4,
    "ic_gate_thresh": 0.03,
    "entropy_collapse_thresh": 0.5,
    "elite_replay_frac": 0.3,
    "max_restarts": 3,
    "llm_provider": "openai",
    "llm_model": "gpt-4-turbo",
    "weight_return": 0.4,
    "weight_ic": 0.3,
    "weight_stability": 0.2,
    "weight_entropy": 0.1
  }
}
```

## 四、分阶段实施路线图

### 第一阶段：基础公式系统（Week 1-2）
- [ ] 在 `core/` 新增 `formula_vocab.py`、`ops.py`、`vm.py`（参考 AlphaMaster 但简化）
- [ ] 新增 `AIFormulaExecutor` 类，能执行简单公式序列
- [ ] 修改策略加载入口：当检测到 `best_{symbol}.json` 时使用 AI 执行器
- [ ] 在 `strategies/` 目录放置一个示例 `best_XAUUSD.json`
- [ ] 验证：AI 策略员能正确执行公式并生成交易信号

### 第二阶段：评估机制升级（Week 3）
- [ ] 在 `engine_standalone/` 新增多维度评估模块
- [ ] 实现 IC 计算（因子 vs 未来N期收益相关系数）
- [ ] 实现稳定性评估（如因子自相关系数衰减）
- [ ] 实现熵评估（策略方向分布的香农熵）
- [ ] 修改引擎评分逻辑：使用加权多维度得分
- [ ] 验证：回测时能看到多维度评分输出

### 第三阶段：AI 自动训练服务（Week 4-5）
- [ ] 新增 `services/ai_strategy_trainer.py`
- [ ] 集成现有 `llm_provider.py` 调用 LLM
- [ ] 实现公式生成 Prompt（参考 AlphaMaster 的系统提示）
- [ ] 实现迭代循环：生成 → 回测 → 分析 → 改进
- [ ] 新增 checkpoint 机制（断点续训）
- [ ] 验证：后台服务能自动运行并更新 best_XAUUSD.json

### 第四阶段：Web 监控与控制（Week 6）
- [ ] 新增后端 API 路由（训练控制、历史查询）
- [ ] 前端页面：训练监控面板（进度条、指标曲线、公式编辑器）
- [ ] 实现配置热加载：修改 runtime_config 自动应用到训练服务
- [ ] 验证：能通过 Web 界面启动/停止训练、查看进度

### 第五阶段：集成验证与优化（Week 7-8）
- [ ] 在纸面交易环境中运行 AI 增强策员
- [ ] 对比原有人工策略 vs AI 生成策略的表现
- [ ] 调整评估权重、公式长度、训练参数
- [ ] 优化 LLM Prompt 以减少无效公式
- [ ] 添加特征重要性分析（哪些指标被 AI 多次选中）
- [ ] 准备上线实盘（纸面验证通过后）

## 五、技术风险与对策

### 风险 1：LLM 生成无效或危险公式
- **对策**：
  - 公式语法验证（在执行前先检查词表合法性）
  - 安全操作符列表（禁止危险操作如除零、越界引用）
  - 公式长度限制（如最大 20 个 token）
  - 执行时异常捕获（无效公式返回中性信号 0）

### 风险 2：训练过程消耗过多计算资源
- **对策**：
  - 后台服务使用进程池限制并行数
  - 回测使用缓存（相同历史数据不重复加载）
  - 评估窗口控制（如只用最近 1000 根 K 线快速评估）
  - 训练步数可配置（默认 30-50 步即可收敛）

### 风险 3：过拟合（在训练数据上表现好，实际不好）
- **对策**：
  - 必须使用 Walk-forward 或时间序列交叉验证
  - 加入过拟合惩罚项（验证得分/训练得分比率）
  - 稳定性评分本身就惩罚过拟合策略
  - 限制公式复杂度（防止过度拟合噪声）

### 风险 4：与现有策略池冲突
- **对策**：
  - AI 策员作为策略池的一种特殊策略（名字如 `AI_Factor_Miner`）
  - 保持原有人工策略可选，可并行运行
  - 通过 `strategy_pool.max_positions` 控制其仓位比例
  - 初期使用小仓位试水，逐步增加

## 六、预期效果

### 短期（1-2 个月）
- 减少策略编写工作量：不再需要手写复杂条件
- 策略更客观：基于数据而非经验主义
- 能快速适应市场变化：AI 可在几小时内重新学习新 regime
- 多维度评估减少灾难性回撤：不仅看收益还看稳定性

### 中期（3-6 个月）
- 策略表现持续改善：AI 持续迭代优化
- 发现人类不易察觉的因子组合：如非线性指标交互
- 建立策略因子库：从训练历史中提取高频有效特征
- 减少人工参数调试：自动找到最优参数组合

### 长期（6 个月以上）
- 形成自我改进的交易系统：越用越聪明
- 可扩展到其他品种：相同框架套用不同历史数据
- 为研究提供平台：可视化因子演化过程
- 最终目标：让系统自动发现并维护盈利策略，人类只需设定目标和约束

## 七、立即可执行的第一步

明天开始，请执行：
1. 创建目录 `core/formula_system/`
2. 复制我们现有的 `data_factory.py` 中的指标列表，建立初始特征词表
3. 定义基础操作符词表（+ - * / > < == REF SMA EMA）
4. 写一个最小的 StackVM 执行器，能处理如 `["rsi", "REF", "1", "<", "30", "mfi", "REF", "1", "<", "25", "*"]` 这样的公式（代表 RSI<30 且 MFI<25）
5. 在 `strategies/` 放置一个测试用的 `best_XAUUSD.json`
6. 修改策略加载入口，当检测到该文件时使用 AI 执行器

这个最小可执行版本将验证我们的思路是否可行，为后续全面 AI 增强奠定基础。

---
*改造计划由 AI 助手生成，基于对 AlphaMaster 方法论的提炼和对 XAUUSD 量化系统现状的分析。*