# 2026-08-29 工作日志

## 制定代码审查标准与流程（CodeReviewExpert）
- 产出：`docs/CODE_REVIEW_STANDARD.md`（v1.0），针对 AlgoForge XAUUSD 交易系统特化。
- 核心思路：以"资金安全 > 可用性 > 安全 > 可维护性"为优先级，分级 🔴/🟡/💭。
- 探查了关键风险代码并作为标准证据落地：
  - `core/freemt4_bridge.py`：报价失败返回 (0.0,0.0)、sl/tp 用 0 作"未设置"哨兵、`_recv_raw` 反复 decode+无长度上限、`_send_cmd` 在途指令失败不重试。
  - `engine_standalone/core_loop.py`：tick 循环内 `time.sleep(20)*3` 阻塞、`_check_news_bias_block` 用 `except: pass` 静默 fail-open。
  - `engine_standalone/risk_mgr.py`：`check_consecutive_loss` 在"判断"中改状态（查询/变更未分离）。
  - `data/database.py`：SQL 基本参数化（良好），但批量写入 `try/except: pass` 静默丢数据。
  - `services/llm_provider.py`：API Key 明文写 JSON（密钥泄露风险），已有 `_sanitize` 脱敏。
- 测试缺口：12 个测试文件集中在 agent/mcp/news_filter，**risk_mgr / bridge / database 交易写入无单测**——列为最大测试缺口。
- 含：评审流程、作者自检清单、pre-commit/CI 配置片段、导师式评论格式。

## 实施①：risk_mgr 查询/变更分离重构 + 单测（用户选"推荐"范围）
- 重构 `engine_standalone/risk_mgr.py`：
  - `check_consecutive_loss(state, pnl, max_consecutive)` 拆为 `register_trade_result(state, pnl)`（仅改状态）+ `check_consecutive_loss(state, max_consecutive)`（纯查询，不改状态）。
  - `check_rapid_exit` 改为纯查询（窗口内计数，不裁剪）；新增 `prune_exit_window` 作独立维护步骤。
  - 确认无外部调用方（引擎 main/position_mgr 是手写 consecutive_losses+=1），重构零风险。
- 更新+补强 `tests/unit/test_risk_mgr.py`：新增 register/纯查询分离测试、连续亏损恰好达阈值、查询函数调用两次不改状态（回归）、快速出场恰好达阈值、窗口裁剪独立测试、已实现亏损%浮点边界。
- 结果：`tests/unit/test_risk_mgr.py` **30 passed**；`test_position_mgr.py`（同用风控状态）21 passed。其余 test_config_schema/test_web_manager 因 venv 缺 pydantic/fastapi 收集报错，与本次改动无关。
- 环境：在隔离 venv `C:\Users\Administrator\.workbuddy\binaries\python\envs\default` 装 pytest 9.1.1 跑测试。
- 后续建议（💭）：引擎 main/position_mgr 的连续亏损逻辑可迁移到 `register_trade_result` 以统一口径。
- 待办（用户待决）：② 桥接层 0 哨兵/(0.0,0.0) 返回/_recv_raw 解码修复；③ 全部 🔴（core_loop 阻塞、新闻 fail-open、密钥明文）。
